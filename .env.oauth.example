# OAuth Configuration Example
# Copy this to .env and fill in your values

# ============================================================================
# REQUIRED: Basic Server Configuration
# ============================================================================
MCP_MODE=http
MCP_HOST=0.0.0.0
MCP_PORT=8080
MCP_SECRET_KEY=<generate with: openssl rand -base64 32>
MCP_ADMIN_TOKEN=<generate with: openssl rand -base64 32>

# ============================================================================
# REQUIRED: OAuth Configuration
# ============================================================================
GITHUB_OAUTH_ENABLED=true

# Get these from: https://github.com/settings/developers
GITHUB_OAUTH_CLIENT_ID=Iv1.abc123...
GITHUB_OAUTH_CLIENT_SECRET=abc123secretxyz...

# ============================================================================
# CALLBACK URL - CHOOSE ONE BASED ON DEPLOYMENT:
# ============================================================================

# Local Development (with ngrok):
# GITHUB_OAUTH_CALLBACK_URL=https://abc123.ngrok.io/auth/callback

# Local Development (localhost - for testing only):
# GITHUB_OAUTH_CALLBACK_URL=http://localhost:8080/auth/callback

# GCP Cloud Run:
# GITHUB_OAUTH_CALLBACK_URL=https://your-service-name-abc123-uc.a.run.app/auth/callback

# Render.com:
# GITHUB_OAUTH_CALLBACK_URL=https://your-app-name.onrender.com/auth/callback

# Custom Domain:
# GITHUB_OAUTH_CALLBACK_URL=https://mcp.yourdomain.com/auth/callback

# ============================================================================
# OPTIONAL: OAuth Scopes
# ============================================================================
# Default scopes: repo,read:user
# For public repos only: public_repo,read:user
# GITHUB_OAUTH_SCOPES=repo,read:user

# ============================================================================
# OPTIONAL: Rate Limiting
# ============================================================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST=10

# ============================================================================
# OPTIONAL: CORS
# ============================================================================
CORS_ENABLED=true
CORS_ALLOW_ORIGINS=*
CORS_ALLOW_CREDENTIALS=true
