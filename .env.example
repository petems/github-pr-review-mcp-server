# GitHub token for accessing private repositories
GITHUB_TOKEN=your_github_token_here
#
# Scopes:
# - Classic PAT: use `public_repo` for public repos or `repo` for private.
# - Fine-grained PAT: grant repository access and enable Pull requests: Read.

# Optional safety/performance settings
# Maximum number of pages to fetch from GitHub
# PR_FETCH_MAX_PAGES=50

# Maximum total comments to fetch before stopping
# PR_FETCH_MAX_COMMENTS=2000

# Items per page for GitHub API (1-100)
# HTTP_PER_PAGE=100

# Max retries for transient errors (network/5xx)
# HTTP_MAX_RETRIES=3
